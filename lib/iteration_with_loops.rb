def find_min_in_nested_arrays(src)
  row_index = 0
  minimum =[]
  while row_index < src.count do
    element_index = 0
    sorted = []
    while element_index < src[row_index].count do
      if src[row_index][element_index].sort = sorted
        shift.sorted = min_val
      end
      element_index += 1
    end
    minimum << min_val
    row_index += 1
  end
end
